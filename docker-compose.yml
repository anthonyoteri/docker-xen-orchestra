version: "3.9"
services:
  db:
    image: redis:5.0.3
  orchestra:
    build: .
    image: registry.anthonyoteri.com/xen-orchestra:latest
    ports:
      - "8443:443"
    volumes:
      - config:/config
    links:
     - db
    environment:
      - REDIS_URI=redis://db:6379/0
volumes:
  config: {}